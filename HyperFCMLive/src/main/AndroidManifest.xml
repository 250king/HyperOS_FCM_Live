<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android package visibility on Android 11+. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- MIUI 13 / HyperOS adds a separate runtime gate on top of
         QUERY_ALL_PACKAGES for getInstalledPackages()/getInstalledApplications(). -->
    <uses-permission android:name="com.android.permission.GET_INSTALLED_APPS" />

    <application
        android:description="@string/xposed_description"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperFCMLive"
        tools:ignore="AllowBackup,MissingApplicationIcon">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
